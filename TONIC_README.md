# TONIC — کلاینت VPN اختصاصی (بر پایه‌ی v2rayNG)

یک کلاینت VPN اندروید برندشده و **خصوصی**: کاربر با یوزر/پسورد وارد می‌شود، اپ
لینک‌های ساب را از بک‌اند می‌گیرد، کانفیگ‌ها را ایمپورت می‌کند و با **یک دکمه** وصل می‌شود.
افزودن/اکسپورت/اشتراک کانفیگ و ساب غیرفعال است.

موتور اتصال (هسته‌ی Xray، سرویس VPN، پارس ساب/کانفیگ) از **v2rayNG** بازاستفاده شده و
فقط UI و منطق ورود داده جایگزین شده است.

## امکانات
- ورود با یوزر/پسورد به بک‌اند (FastAPI تستی همراه پروژه).
- گرفتن لیست ساب از بک‌اند و ایمپورت خودکار کانفیگ‌ها.
- آپدیت خودکار هر **۱۲ ساعت** (لیست ساب‌ها + کانفیگ‌های هر ساب).
- UI مینیمال: یک دکمه‌ی بزرگ اتصال + انتخاب اختیاری لوکیشن.
- کلاینت قفل‌شده: بدون افزودن/اکسپورت/QR/مدیریت ساب.
- **ری‌برند کامل از یک فایل** (`V2rayNG/brand/brand.properties`): نام، پکیج، رنگ، آدرس بک‌اند، فلگ‌ها.

## مستندات
- ساخت اپ (قدم‌به‌قدم ویندوز + CI): [`docs/tonic/BUILD.md`](docs/tonic/BUILD.md)
- ری‌برند و تم: [`docs/tonic/REBRAND.md`](docs/tonic/REBRAND.md)
- معماری (چه چیزی بازاستفاده/جدید/قفل شد): [`docs/tonic/ARCHITECTURE.md`](docs/tonic/ARCHITECTURE.md)
- بک‌اند و قرارداد API: [`docs/tonic/BACKEND.md`](docs/tonic/BACKEND.md)

## شروع سریع
1. **بک‌اند** را بالا بیاور:
   ```bash
   cd backend && pip install -r requirements.txt
   uvicorn main:app --host 0.0.0.0 --port 8000 --reload
   ```
2. **APK** بگیر — ساده‌ترین راه: پوش به GitHub و اجرای workflow «TONIC Build» در تب Actions
   (جزئیات در `docs/tonic/BUILD.md`). یا لوکال با Android Studio.
3. اپ را روی **امولاتور** نصب کن، با `demo` / `demo123` وارد شو و دکمه‌ی اتصال را بزن.

> کانفیگ‌های بک‌اند تستی به سرور واقعی وصل نمی‌شوند (فقط برای تست جریان‌اند). برای اتصال
> واقعی، در بک‌اند لینک ساب واقعی برگردان (`docs/tonic/BACKEND.md`).

## برنچ
همه‌ی تغییرات روی برنچ `tonic-client` هستند (جدا از `master` که همان v2rayNG اصلی است).
